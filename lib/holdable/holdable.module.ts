import { NgModule } from '@angular/core';
import { HoldableDirective } from './holdable.directive';

@NgModule({
    imports: [],
    declarations: [HoldableDirective],
    exports: [HoldableDirective],
    providers: []
})
export class HoldableDirModule {}